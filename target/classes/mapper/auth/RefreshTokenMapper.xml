<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.project.api.v1.auth.refreshToken.mapper.RefreshTokenMapper">

    <insert id="insertRefreshToken"
            parameterType="com.ssafy.project.api.v1.auth.refreshToken.dto.RefreshTokenDto"
            useGeneratedKeys="true"
            keyProperty="tokenId">
        INSERT INTO refresh_tokens (
            user_id,
            token_hash,
            expires_at
        ) VALUES (
            #{userId},
            #{tokenHash},
            #{expiresAt}
        )
    </insert>

    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM refresh_tokens
        WHERE user_id = #{userId}
    </delete>

    
    <resultMap id="RefreshTokenResultMap"
               type="com.ssafy.project.api.v1.auth.refreshToken.dto.RefreshTokenDto">
        <id     property="tokenId"   column="token_id"/>
        <result property="userId"    column="user_id"/>
        <result property="tokenHash" column="token_hash"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByTokenHash"
            parameterType="string"
            resultMap="RefreshTokenResultMap">
        SELECT token_id, user_id, token_hash, expires_at, created_at
        FROM refresh_tokens
        WHERE token_hash = #{tokenHash}
    </select>

</mapper>
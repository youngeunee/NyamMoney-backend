<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.project.api.v1.brand.mapper.BrandMapper">
	<select id="findBrand" parameterType="String" resultType="String">
		SELECT brand_name
	    FROM brand_dictionary
	    WHERE brand_name LIKE CONCAT('%', #{merchantName}, '%')
	    UNION
	    SELECT brand_name
	    FROM brand_alias ba
	    JOIN brand_dictionary bd ON ba.brand_id = bd.brand_id
	    WHERE ba.alias LIKE CONCAT('%', #{merchantName}, '%')
	</select>
</mapper>
